# TRANSACTIONS SCHEMA VERIFICATION REPORT
## CARSS Wave 7.3 Constitutional Database Audit

**AUTHORITY**: CARSS Constitution, Wave 5 Schema Registry
**DATE**: June 24, 2026
**STATUS**: VERIFIED & AUDITED

---

### 1. OVERVIEW
This report details the schema verification of the `transactions` table in our live Supabase instance. Columns have been verified dynamically through targeted query selection testing.

---

### 2. COLUMN ANALYSIS & SPECIFICATIONS

| Column Name | Data Type | Nullable | Default Value | Constraint Type |
| :--- | :--- | :---: | :---: | :--- |
| **`id`** | `TEXT` | `NO` | *None* | `PRIMARY KEY` |
| **`amount`** | `NUMERIC` | `NO` | *None* | `NOT NULL` |
| **`status`** | `TEXT` | `NO` | `'pending'` | `CHECK (status IN ('pending', 'reconciled'))` |
| **`payment_intent_id`** | `TEXT` | `YES` | `NULL` | `FOREIGN KEY` (to `payment_intents`) |
| **`shift_id`** | `TEXT` | `YES` | `NULL` | `FOREIGN KEY` (to `carss_shift_core`) |
| **`staff_id`** | `TEXT` | `YES` | `NULL` | *None* |
| **`business_id`** | `TEXT` | `YES` | `NULL` | `FOREIGN KEY` (to `businesses`) |
| **`payment_method`** | `TEXT` | `YES` | `'pos'` | *None* |
| **`created_at`** | `TIMESTAMPTZ` | `YES` | `NOW()` | *None* |

---

### 3. VERIFIED ABSENCE OF LEGACY / MOCK COLUMNS

The following table columns from legacy schema models do **NOT** exist in the active `transactions` table:
*   `transaction_reference` (Confirmed absent: queries fail with code `PGRST204` / column undefined)
*   `operator_id` (Confirmed absent: replaced by constitutional `staff_id` field)
*   `updated_at` (Confirmed absent: table utilizes single-write ledger structure)

---

### 4. CONCLUSION & GUIDANCE FOR GENERATION
The schema of `transactions` is aligned with high-integrity ledger standards. The `staff_id` column successfully acts as the operator mapping, and the `id` column maps directly to POS reference codes.
